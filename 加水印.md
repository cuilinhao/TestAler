

按照下面要求来实现


你写代码，要先梳理现在的代码逻辑，要全面一点，尽量不要再出现问题，
你改代码，要尽量用用最少的代码来完成任务，达到目的，明白吗？

你写的代码要加简单明确注释




# 核心架构与交互逻辑
1. 面板内部需要有一个导航栈（使用 NavigationStack）（就是弹框B，对应代码中的CameraSettingsSheet）
2. **页面跳转（Push/Pop）：** 当点击“水印”按钮时，面板内容向左平滑推入（Push）**【水印设置页面】**。当在【水印设置页面】点击左上角的“< 水印”时，向右滑出（Pop）回到主设置页面（弹框B）。

# 【水印设置页面】详细 UI 需求（重点还原 如截图2）
这个页面需要严格按照以下描述还原：

**1. 整体背景与顶部结构：**
- 背景色：深色/接近黑色（如 `Color(UIColor.secondarySystemBackground)` 的深色模式或自定义深灰色）。
- 顶部居中：一个细长的胶囊体，作为拖拽指示条（Drag Handle）。
- 顶部导航栏（使用自定义 HStack，不要使用系统默认的大标题 NavigationBar）：
  - 左侧：一个返回箭头 `<` 加上文字 `水印`，字体颜色为白色，点击触发 Pop 返回上一页。
  - 右侧：一个系统原生的 Switch Toggle 开关，开启状态的 `tintColor` 设为亮黄色/青柠色（类似 #D4FF59）。

**2. 核心内容区（横向滚动的卡片列表）：**
- 使用水平滑动的 `ScrollView(.horizontal, showsIndicators: false)`。
- 卡片之间有适当的间距（spacing: 16），右侧需要露出下一个卡片的边缘，暗示可以滑动。

**3. 单个水印卡片样式（Item View）：**
- 尺寸比例：横向长方形（类似 16:9 比例的缩小版）。
- 卡片分为上下两部分：
  - **上半部分（占大部分高度）：** 预览图片（可以使用 `Color.gray` 或从 Assets 中读取的占位图来代替雪山风景）。图片要有 `clipped()` 并且顶部有小圆角。
  - **下半部分（底部信息栏）：** 白色背景，高度较窄。
    - 左侧内容：一个小图标（如苹果 Logo，可使用 SF Symbol `applelogo`）紧挨着文字 "iPhone 17 Pro Max"，黑色细体字。
    - 右侧内容：相机参数文字 "50mm f/1.78 1/80s ISO 640"，灰色细体字，字号比左侧略小。
- 卡片整体要有圆角（cornerRadius: 8）。

**4. 卡片选中状态（Selection State）：**
- 需要维护一个 State 变量来记录当前选中的卡片 Index。
- **高亮表现：** 被选中的卡片外围需要有一圈 **亮黄色/青柠色的描边**（使用 `RoundedRectangle().stroke()`，线宽约 2-3 pt）。未选中的卡片没有这个描边。


